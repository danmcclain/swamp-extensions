# BEGIN swamp managed section - DO NOT EDIT

# Runtime data (not needed in version control)
.swamp/

# Local extension sources (developer-specific, not shared)
.swamp-sources.yaml
# END swamp managed section

# Pulled dependency artifacts (regenerated by `swamp extension pull`; see manifest `dependencies`)
.claude/skills/
extensions/models/upstream_extensions.json

# Deno lockfile — extension npm deps are bundled at bundle time, not lockfile-tracked
deno.lock
